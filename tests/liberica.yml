---
- include_role:
    name: "{{ playbook_dir.split('/')[:-1] | last }}"
  vars:
    version: "{{ item }}"
    flavor: liberica
  loop: "{{ flavor_support[eff_flavor].versions }}"
  when: ansible_os_family | lower in flavor_support[eff_flavor].families
